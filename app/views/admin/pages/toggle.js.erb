if ('<%= @error %>'){
	alert("Something went wrong when you didn't expect it!");
}
else{
	$("#page_item_<%= @page.id %>").replaceWith('<%= escape_javascript(render(:partial => "item", :object => @page)) %>');
}