$(document).ready(function() {
	var $dialog = $('<%= escape_javascript(render(:partial => "form", :object => @upload, :as => :upload)) %>')
		.dialog({
			autoOpen: true,
			title: 'Edit upload \"<%= @upload.data_file_name %>\"'
		});
});
