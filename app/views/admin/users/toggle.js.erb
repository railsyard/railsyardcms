if ('<%= @error %>'){
	alert("Something went wrong when you didn't expect it!");
}
else{
	$("#user_item_<%= @user.id %>").replaceWith('<%= escape_javascript(render(:partial => "item", :object => @user)) %>');
}